<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.open_day_project">

    <uses-feature
        android:name="android.hardware.Sensor.proximity"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".humdity"></activity>
        <activity android:name=".pressure" />
        <activity android:name=".temp" />
        <activity android:name=".light" />
        <activity android:name=".magn" />
        <activity android:name=".RGB" />
        <activity android:name=".Gyroscope" />
        <activity android:name=".Accelerometer" />
        <activity android:name=".proxim" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>